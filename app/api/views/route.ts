import { recordAnalyticsEvent } from "../../../src/lib/cms/viewAnalytics";
export async function POST(request: Request) { try { const body = await request.json().catch(() => ({})) as any; await recordAnalyticsEvent({ eventType: body.eventType || body.event_type || "page_view", path: body.path || new URL(request.url).pathname, title: body.title, target: body.target || body.target_element, referrer: body.referrer, userAgent: request.headers.get("user-agent") || "", sessionId: body.sessionId || body.session_id, visitorId: body.visitorId || body.visitor_id, country: request.headers.get("x-country") || request.headers.get("x-nf-country") || "" }); return Response.json({ ok: true }); } catch (error) { return Response.json({ ok: false, error: error instanceof Error ? error.message : "Unable to record analytics event." }, { status: 400 }); } }
